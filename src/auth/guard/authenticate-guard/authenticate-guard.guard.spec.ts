import { AuthenticateGuardGuard } from './authenticate-guard.guard';

describe('AuthenticateGuardGuard', () => {
  it('should be defined', () => {
    expect(new AuthenticateGuardGuard()).toBeDefined();
  });
});
